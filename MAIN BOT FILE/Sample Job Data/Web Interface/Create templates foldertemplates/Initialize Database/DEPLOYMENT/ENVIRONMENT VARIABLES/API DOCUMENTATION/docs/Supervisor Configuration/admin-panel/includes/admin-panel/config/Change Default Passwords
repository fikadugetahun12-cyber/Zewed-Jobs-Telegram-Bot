# Generate password hash for admin user
php -r "echo password_hash('new_password', PASSWORD_DEFAULT);"

# Update in database
mysql -u root -p zewedjobs_admin -e "
UPDATE admin_users SET password_hash='\$2y\$10\$GeneratedHashHere' WHERE username='admin';
"
