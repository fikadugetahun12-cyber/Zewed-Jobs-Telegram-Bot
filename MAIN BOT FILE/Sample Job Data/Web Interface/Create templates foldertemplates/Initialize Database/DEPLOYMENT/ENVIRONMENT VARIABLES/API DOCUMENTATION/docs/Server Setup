# Update system
sudo apt update && sudo apt upgrade -y

# Install required software
sudo apt install -y python3 python3-pip python3-venv \
    php php-fpm php-mysql nginx mysql-server \
    git supervisor certbot
